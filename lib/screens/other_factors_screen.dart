import 'package:flutter/material.dart';

class OtherFactorsScreen extends StatelessWidget {
  const OtherFactorsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Other Factors'),
      ),
      body: const Center(
        child: Text('Other Factors Screen'),
      ),
    );
  }
}